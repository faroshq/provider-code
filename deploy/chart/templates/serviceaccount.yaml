{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "code.serviceAccountName" . }}
  labels:
    {{- include "code.labels" . | nindent 4 }}
{{- end }}
